# =====================================================
# Anthropic Claude API
# Obter em: https://console.anthropic.com/settings/keys
# =====================================================
ANTHROPIC_API_KEY=sk-ant-api03-...

# Modelos a usar (não precisa mexer)
ANTHROPIC_MODEL_TRIAGE=claude-haiku-4-5-20251001
ANTHROPIC_MODEL_DEEP=claude-sonnet-4-6

# =====================================================
# YouTube Data API v3 (para discover e leitura)
# Obter em: https://console.cloud.google.com/apis/credentials
#   1. Criar projeto
#   2. Ativar "YouTube Data API v3"
#   3. Criar credencial "API Key" (sem restrições no início)
# =====================================================
YOUTUBE_API_KEY=AIzaSy...

# =====================================================
# YouTube OAuth (para upload na conta do canal)
# Obter em: https://console.cloud.google.com/apis/credentials
#   1. Criar credencial "OAuth client ID"
#   2. Application type: Desktop app
#   3. Baixar JSON e salvar no caminho abaixo
#   4. Adicionar sua conta como "test user" em OAuth consent screen
# =====================================================
YOUTUBE_OAUTH_CLIENT_SECRETS_PATH=/home/youruser/.config/canal-soberania/client_secrets.json
YOUTUBE_OAUTH_TOKEN_PATH=/home/youruser/.config/canal-soberania/youtube_token.json

# =====================================================
# Whisper (faster-whisper)
# =====================================================
WHISPER_MODEL=large-v3
WHISPER_DEVICE=cpu          # ou "cuda" se tiver GPU
WHISPER_COMPUTE_TYPE=int8   # cpu: int8; cuda: float16

# =====================================================
# Diretórios (relativos ao repo, normalmente não mexer)
# =====================================================
DATA_DIR=./data

# =====================================================
# Alertas operacionais
# ALERT_CHANNELS: canais ativos, comma-separated: "telegram,smtp" | "telegram" | "smtp" | "none"
# =====================================================
ALERT_CHANNELS=telegram
ALERT_STUCK_THRESHOLD=50

# Telegram Bot
# Criar bot em @BotFather, pegar token e chat_id do seu usuário/grupo
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# SMTP (email — opcional, ative adicionando "smtp" ao ALERT_CHANNELS)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu@email.com
SMTP_PASSWORD=app_password_aqui
SMTP_FROM=seu@email.com
SMTP_TO=destino@email.com

# =====================================================
# Backends plugáveis (Onda 1)
# LLM_BACKEND: anthropic | ollama | hybrid | openai
#   anthropic: Claude direto (padrão)
#   hybrid: Qwen 14B via Ollama para triagem + Sonnet para análise profunda
#   ollama: Qwen via Ollama para todos os stages
# WHISPER_BACKEND: local_cpu | local_cuda | groq | openai
# =====================================================
LLM_BACKEND=anthropic
WHISPER_BACKEND=local_cpu

# Ollama (necessário se LLM_BACKEND=ollama|hybrid)
# Instalar: https://ollama.com/download
# Modelos: ollama pull qwen2.5:14b-instruct-q4_K_M
#          ollama pull qwen2.5:32b-instruct-q4_K_M
OLLAMA_BASE_URL=http://localhost:11434/v1/chat/completions
OLLAMA_MODEL_TRIAGE=qwen2.5:14b-instruct-q4_K_M
OLLAMA_MODEL_DEEP=qwen2.5:32b-instruct-q4_K_M

# APIs de escape hatch (opcionais)
# Groq: necessário se WHISPER_BACKEND=groq (transcrição cloud gratuita)
GROQ_API_KEY=
# OpenAI: necessário se WHISPER_BACKEND=openai ou LLM_BACKEND=openai
OPENAI_API_KEY=
# OpenRouter: necessário para modelos não-Claude via LLMClient legado
OPENROUTER_API_KEY=

# =====================================================
# API REST (Onda 2)
# Token gerado automaticamente em DATA_DIR/.api_token na 1ª execução de cs serve.
# Não definir aqui — o arquivo .api_token tem chmod 600.
# Para expor na LAN: cs serve --host 0.0.0.0
# Referência de endpoints: docs/api.md
# =====================================================
API_HOST=127.0.0.1
API_PORT=8000

# =====================================================
# Comportamento
# =====================================================
LOG_LEVEL=INFO              # DEBUG | INFO | WARNING | ERROR
DRY_RUN=false               # true = não executa side effects (download, upload)
